{
  "name": "@dovigod/monorepo-boilerplate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:a": "pnpm --filter a build",
    "cache-clean": "npm cache clean --force",
    "lerna": "lerna",
    "nx:build": "pnpm exec nx run-many --target=build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "packageManager": "^pnpm@8.6.0",
  "dependencies": {
    "lerna": "^7.4.2",
    "nx": "^17.1.2"
  },
  "devDependencies": {
    "nx": "17.1.2"
  }
}
