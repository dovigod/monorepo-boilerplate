{
  "name": "monorepo-boilerplate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:a": "pnpm --filter a build",
    "cache-clean": "npm cache clean --force"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "packageManager": "^pnpm@8.6.0"
}
